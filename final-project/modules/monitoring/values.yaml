defaultRules:
  create: true

grafana:
  enabled: true
  adminPassword: "${admin_password}"
  service:
    type: LoadBalancer
  persistence:
    enabled: true
    storageClassName: "gp2"
    size: 5Gi

prometheus:
  prometheusSpec:
    serviceMonitorSelectorNilUsesHelmValues: false